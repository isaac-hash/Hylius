{
  "name": "hylius-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/cli && npm run build -w apps/dashboard",
    "dev": "npm run dev -w packages/core && npm run dev -w packages/cli && npm run dev -w apps/dashboard",
    "test": "npm run test -w packages/core && npm run test -w packages/cli && npm run test -w apps/dashboard"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}